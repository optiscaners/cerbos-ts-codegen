apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: fleet_attribute
  rules:
  
    - actions: [read]
      effect: EFFECT_ALLOW
      roles: [user]
  
    - actions: ["*"]
      effect: EFFECT_ALLOW
      roles: [admin]
  
  

    
